% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/peptide_utils.R
\name{find_peptide_pos}
\alias{find_peptide_pos}
\title{查找肽段在蛋白质序列中的位置}
\usage{
find_peptide_pos(peptides, protein_seq)
}
\arguments{
\item{peptides}{字符向量。待查找的肽段序列，例如 \code{c("AAPPL", "SKAEYLK")}。}

\item{protein_seq}{字符串。目标蛋白质序列，例如 \code{"MAAAAPPLSKAEYLK"}。}
}
\value{
一个 \code{data.table}，包含以下三列：
\describe{
\item{\code{peptide}}{字符型。输入的肽段序列。}
\item{\code{start}}{整数型。肽段在蛋白质中的起始位置（1-based）；未匹配时为 \code{NA}。}
\item{\code{end}}{整数型。肽段在蛋白质中的终止位置（1-based）；未匹配时为 \code{NA}。}
}

若一个肽段在蛋白质中出现多次，则每次匹配各占一行。
}
\description{
在单条蛋白质序列中批量查找多个肽段的起止位置。
对未匹配的肽段返回 \code{NA} 而非报错，适合大规模蛋白质组学数据处理。
}
\details{
函数内部流程：
\enumerate{
\item 将蛋白质序列转换为 \code{Biostrings::AAString} 对象
\item 将所有肽段转换为 \code{Biostrings::AAStringSet} 对象
\item 对每个肽段调用 \code{Biostrings::matchPattern}（C 底层实现）进行精确匹配
\item 使用 \code{data.table::rbindlist} 合并结果（优于 \code{do.call(rbind)}）
}

所有外部函数均通过 \code{::} 显式调用，无需在 \code{NAMESPACE} 中声明 \code{importFrom}，
仅需在 \code{DESCRIPTION} 的 \code{Suggests} 或 \code{Imports} 字段中列出依赖包即可。

\strong{注意事项：}
\itemize{
\item 匹配区分大小写，输入序列须为标准单字母氨基酸编码（大写）
\item 不支持含修饰标注的序列（如 \code{"AAC(UniMod:4)K"}），请预先清洗
\item 函数每次只处理一条蛋白质序列；多蛋白场景请在外部循环调用
}
}
\examples{
protein <- "MAAAAPPLSKAEYLKGKR"

peptides <- c(
  "AAPPL",      # 正常匹配
  "SKAEYLK",    # 正常匹配
  "ZZZZZ",      # 不存在，返回 NA
  "GKR"         # 正常匹配
)

find_peptide_pos(peptides, protein)
#>    peptide start end
#> 1:   AAPPL     3   7
#> 2: SKAEYLK     9  15
#> 3:   ZZZZZ    NA  NA
#> 4:     GKR    16  18

# 多蛋白场景：在外部循环调用
protein_list <- c(prot1 = "MAAAAPPLSK", prot2 = "MGKRSKAEYLK")

result <- do.call(rbind, lapply(names(protein_list), function(pid) {
  df <- find_peptide_pos(peptides, protein_list[[pid]])
  df$protein <- pid
  df
}))

}
\seealso{
\code{\link[Biostrings]{matchPattern}} 底层匹配函数
}
